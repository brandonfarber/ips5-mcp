# Issue log



| Date (UTC) | Severity | Summary | Status |

| ---------- | -------- | ------- | ------ |

| 2026-05-12 | info | Agent sandbox: `npm` not on PATH during initial scaffold; local verification required. | Closed |

| 2026-05-12 | info | `core_hello` tool added; API key must be permitted for **GET /core/hello** in ACP. | Open |
| 2026-05-12 | info | Project `.cursor/mcp.json` uses `envFile` → `.env`. If MCP fails to start, confirm `.env` exists or remove `envFile` and set `IPS5_*` in the MCP `env` block. | Open |



## Resolved



- Initial stub shipped without tools; superseded by `core_hello` + `IpsRestClient`.


